build:
    environment:
        go: 'go1.9.2'
    # tests:
    #     override:
    #         -
    #             command: go test -coverprofile=cover.out
    #             coverage:
    #                 file: 'cover.out'
    #                 format: 'go-cc'        